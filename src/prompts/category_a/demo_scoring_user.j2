请对以下Demo课内容进行多维度评分与评审：

## 评分维度
{{ input.scoring_dimensions | join('、') }}

## 用户需求
{{ input.query }}

## Demo课内容
{{ input.demo_content }}

{% if upstream %}
## 上游参考（原始设计方案等）
{{ upstream | tojson(indent=2) }}
{% endif %}

请按以下结构输出评审报告：

### 一、各维度评分（满分100分）

对每个维度进行独立评分：

| 维度 | 得分 | 评级 | 简要说明 |
|------|------|------|----------|
{% for dim in input.scoring_dimensions %}
| {{ dim }} | /100 | | |
{% endfor %}

### 二、综合评分
- 加权总分（各维度等权重计算）
- 综合评级：A（90-100）/ B（75-89）/ C（60-74）/ D（<60）
- 一句话总评

### 三、详细评审意见

对每个维度给出详细评审：
{% for dim in input.scoring_dimensions %}

#### {{ loop.index }}. {{ dim }}
- **优点**：具体列举做得好的地方
- **不足**：具体指出需要改进之处
- **改进建议**：可操作的优化方向
{% endfor %}

### 四、关键改进优先级
按重要性排序列出最需要改进的3-5个要点，标注：
- 改进难度（高/中/低）
- 预期提升幅度（高/中/低）
- 建议优先级（P0/P1/P2）

### 五、总体建议
- 是否达到可交付标准
- 下一步行动建议